apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: exercises
  name: status-pong-claim
spec:
  storageClassName: my-status-pong-log
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
